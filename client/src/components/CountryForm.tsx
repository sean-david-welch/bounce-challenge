import { useState, useEffect } from 'react';

const CountryForm: React.FC = () => {
  return (
    <dialog>
      <form>
        <h1>Form</h1>
      </form>
    </dialog>
  );
};

export default CountryForm;
